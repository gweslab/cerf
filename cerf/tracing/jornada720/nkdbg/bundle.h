#pragma once

#include <cstdint>

/* HP Jornada 720 HPC2000 ROM (bundled/devices/jornada720/jornada720.bin),
   CRC from the [TRACE] bundle line at boot. */
constexpr uint32_t kBundleCrc32 = 0x5FCD433A;
